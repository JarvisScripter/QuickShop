<div class="my-8 flex flex-col items-center">

    <h1 class="text-3xl font-meduim text-gray-900 text-center my-8 md:my-16">{{ page.title }}</h1>
    <div class="max-w-7xl mx-auto px-4">{{ page.content }}</div>
</div>